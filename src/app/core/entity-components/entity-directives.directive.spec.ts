import { EntityDirectivesDirective } from './entity-directives.directive';

describe('EntityDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
