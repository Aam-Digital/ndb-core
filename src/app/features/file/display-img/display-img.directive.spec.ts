import { DisplayImgDirective } from './display-img.directive';

describe('DisplayImgDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayImgDirective();
    expect(directive).toBeTruthy();
  });
});
